# 更新记录

## 0.1.0-alpha.13

- 默认使用浅色外观，并在“更多 → 外观”保留深色与跟随系统；Side Terminal、工作台、助手、协作与讨论界面同步切换。
- 统一文字、边框、表单和状态提示，改善原生终端中低对比度文字的可读性。
- 压缩展开工作台的重复标题，适配矮窗格中的任务输入；Shell 与 AI 入口直接进入各自视图。
- 切换外观保留原终端、输出与草稿，不重新启动任务；外观偏好保存在当前浏览器中。
- 修正 SSH 就绪确认，让连接和重连在远端可接收输入后再开放操作。
- 完成 macOS arm64 与 Linux x86_64 远端实机验收，覆盖连接、尺寸调整、断线重连和任务清理；远端模型执行、额度与跨 DSH 重启续接不在本次验收范围内。
- 更新 README 与官网的浅色工作台和 Side Terminal 截图，保留历史 AI 实测截图的版本说明。

## 0.1.0-alpha.12

- 在现有新终端入口选择本机或 SSH 主机，检查连接、实际目录与远端可启动的 Agent CLI。
- 远端任务由独立 tmux 会话保留；断线后接回原进程，重新取得输入控制权，不重跑任务或重发输入。
- 明确显示执行主机与目录，区分收起、重连和结束远端任务；保留快速退出 CLI 的报错输出。
- 远端记录不会被恢复成本机任务，配置改变时不自动连接新的目的地。
- 远端支持原生 Shell / Agent CLI 和解释建议；DSH AI 执行、自动交接与讨论组仍限本机，避免误操作本机文件。
- 复用本机 OpenSSH 配置，要求已有的免交互认证、可信主机与远端 tmux；来源会话需已有 Full access 权限，不自动更改权限。
- 完成单元回归、真实 SSH/PTY 隔离验收与官方 DSH 安装包验证。外部主机兼容性及跨 DSH 重启续接尚未验收。

## 0.1.0-alpha.11

- 收拢讨论组重复标题与成员设置，为侧栏中的讨论正文留出更多空间。
- 明确标注已关闭与暂不可用的成员，发送前提示处理，保留原有草稿与讨论记录。
- 展示真实的回复与保存进度，减少过期提示对当前操作的干扰。
- 将智能体管理、Supervisor 和布局设置收纳到“更多”，保留常用操作。
- 从讨论或协作返回终端时收起遮挡面板；助手草稿直接显示在目标终端，方便核对和复制。
- 修复返工刚完成时立即验收偶尔被拒绝的问题；结果保存期间继续显示处理中。

## 0.1.0-alpha.8

- 新建终端默认进入 AI 任务，用自然语言驱动 DSH 的文件、编辑与命令工具。
- 按终端保留执行上下文，可追问、执行中追加要求，以及停止执行和后台资源。
- 工具步骤与结果显示在任务记录中，Shell 与智能体 CLI 保留原有终端视图。
- 显示实际模型与权限，显式共享选区，不自动把任务发回来源对话。
- 未确认的发送通过原请求核对，保留新草稿，避免重复执行。
- 独立工作台沿用来源智能体配置，并校验工作区、权限与配置身份。
- 105 项测试通过、0 跳过；真实 DSH 模型完成文件修复与测试，验证了追问、追加要求、进程停止、只读拒绝、草稿隔离及请求去重。

## 0.1.0-alpha.7

- 选中终端文字即可解释、建议修复或交给 Agent，预览并确认后发送。
- 按对话与终端保留助手问题、建议、共享选择和命令草稿；刷新页面后清除。
- 增加 Shell 真实命令记录，显示输出、用时和退出状态。
- 增加明确验收与关联返工，保留原目标、完成判据和上次结果；来源终端关闭后仍可继续。
- 智能体目录分开显示安装、登录、历史模型连接与额度，注明检查时间，无法确认时显示未知。
- 集中显示连接恢复状态，Side Terminal 每次突出一个辅助面板。
- 82 项测试通过、0 跳过；官方 DSH 0.1.1-rc.2 隔离安装验证了真实 PTY、任务隔离、验收与返工去重、取消和冷恢复。

## 0.1.0-alpha.4

- 统一面向用户的界面提示与使用说明。
- 连接、启动和输入失败时显示下一步操作，不再直接展示底层错误。
- 简化账号、订阅和运行状态的表达，保留未确认状态。
- 精简发布说明，优化搜索框细节。

## 0.1.0-alpha.3

- 正式使用 DSH SuperTerminal 名称，兼容原有插件安装。
- 重新设计多终端工作台、智能体目录和右侧助手。
- 增加本机智能体检测、可识别版本与智能体图标。
- 增加 DSH 智能建议与可单独复制的命令草稿。
- 支持在当前工作区启动 Kimi 和 Pi，并使用独立配置。

## 0.1.0-alpha.2

- 增加可拖动的分屏布局与单格放大。
- 增加 DSH 输入栏和 /terminal 入口。
- 支持会话内独立终端与关闭清理。

当前支持 macOS、Node.js 24+ 和官方 DSH 0.1.1-rc.2。登录检查与历史连接证据不保证当前在线；套餐和剩余额度请在相应智能体中查看。
